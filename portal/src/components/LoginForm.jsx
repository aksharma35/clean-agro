/*
 * PLACEHOLDER — not yet implemented.
 *
 * This component is intentionally empty. It is the deliverable for the
 * farmer sign-in ticket, and gets built from the acceptance criteria on
 * that ticket.
 *
 * Contract expected by App.jsx:
 *   props.onSignedIn(farmer)  — call with the resolved farmer object
 *                               returned by signIn() in ../api/auth.js
 *
 * Everything else — markup, validation, error handling, submit state —
 * comes from the ticket.
 */

export default function LoginForm() {
  return (
    <div className="card">
      <div className="stub">
        <div className="ticket">Awaiting implementation</div>
        <p>The farmer sign-in form has not been built yet.</p>
      </div>
    </div>
  );
}
